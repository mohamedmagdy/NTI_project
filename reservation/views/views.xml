<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="reservation.list">
      <field name="name">reservation list</field>
      <field name="model">reservation.reservation</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="reservation.action_window">
      <field name="name">reservation window</field>
      <field name="res_model">reservation.reservation</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="reservation.action_server">
      <field name="name">reservation server</field>
      <field name="model_id" ref="model_reservation_reservation"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="reservation" id="reservation.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="reservation.menu_1" parent="reservation.menu_root"/>
    <menuitem name="Menu 2" id="reservation.menu_2" parent="reservation.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="reservation.menu_1_list" parent="reservation.menu_1"
              action="reservation.action_window"/>
    <menuitem name="Server to list" id="reservation" parent="reservation.menu_2"
              action="reservation.action_server"/>
    -->
  </data>
</odoo>